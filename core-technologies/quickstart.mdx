---
title: "Resources"
description: "Spring handles resources và cách sử dụng resources trong Spring"
---

Trong Java, có `java.net.URL` class có thể xử lý nhiều dạng URL nhưng nó có một số hạn chế như không có cơ chế truy cập resource từ classpath hoặc tương đối từ `ServletContext` , hay chưa có method hỗ trợ kiểm tra sự tồn tại của resource được cung cấp,...

## `Resource` Interface

Spring’s `Resource` interface trong `org.springframework.core.io.` package được mở rộng để hỗ trợ khả năng truy cập vào low-level resources.

Spring cung cấp một số `Resource` implementations:

- `UrlResource`
- `ClassPathResource`
- `FileSystemResource`
- `PathResource`
- `ServletContextResource`
- `InputStreamResource`
- `ByteArrayResource`

## `ResourceLoader` Interface

Spring’s `ResourceLoader` interface được implement để trả về các `Resource` instances thông qua method `getResource`

<Accordion title="Resource strings (Các prefix hỗ trợ tạo Resource objects)">
| Prefix     | Example     | Explanation     |
| --------- | --------- | --------- |
| classpath: | `classpath:com/myapp/config.xml` | Load file từ classpath |
| file: | `file:///data/config.xml` | Load dưới dạng `URL` từ filesystem |
| https: | `https://myserver/logo.png` | Load dưới dạng `URL` |
| (none) | `/data/config.xml` | Tùy thuộc vào loại `ApplicationContext` |
</Accordion>